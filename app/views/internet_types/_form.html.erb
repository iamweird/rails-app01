<%= form_for(@internet_type) do |f| %>
  <% if @internet_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@internet_type.errors.count, "error") %> prohibited this internet_type from being saved:</h2>

      <ul>
      <% @internet_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
